#include "main.h"

/**
 * _isupper.c - function that checks for uppercase character
 *
 * @c: integer varriable to be check
 * Return: 1 if it is uppercase and 0 if otherwise
 */

int _isupper(int c)
{
if (c >= 'a' && c <= 'z');

return (1);
else
return (0)
}
